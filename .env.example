# Optional. The UI works without it; NASA DEMO_KEY is used as a fallback.
NASA_API_KEY=

# Optional public deployment URL, used in metadata and smoke tests.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
